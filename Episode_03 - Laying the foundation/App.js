import React from "react"
import ReactDOM from "react-dom/client"

// This is a React Element
const headind = <h1>React Element</h1>

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(headind)